fn avg a b => a + b / 2;
fn add first second => first + second;
fn compute x y z => ( 2 * 3 * x + 5 * y - 3 * z ) / (1 + 3 + 2 * 2);
pub fn test a b => {
    let var = 2 + 3;
    return add(var * 3, 4);
}
fn test a b => {
    let var = 2 + 3
    add(var * 3, 4)
}